# Dependencies
node_modules/

# Environment files — NEVER commit real secrets
.env
.env.local
.env.production
.env.staging
*.env

# Build output
dist/
build/

# Terraform state and local files — contain real infra state
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
*.tfvars
*.tfplan

# Logs
*.log
logs/

# k6 temp output
/tmp/k6-result-*.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Test coverage
coverage/

# Kubernetes secrets with real values — apply directly with kubectl, never commit
# k8s/secrets.yaml is committed as a TEMPLATE only (no real values)
kubeconfig
*.kubeconfig
